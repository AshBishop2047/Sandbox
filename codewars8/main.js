function invert(array) {
    return array = array.map(num => -num);
 }
 // returns a new array via the map method
 // turns it into a negative number by simply returning the number followed by a minus symbol