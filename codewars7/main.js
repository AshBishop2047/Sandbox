function past(h, m, s){
    return (h*3600000) + (m*60000) + (s*1000)
    }
    // find the value of milliseconds for each individual parameter;
    // then add together each paramater's value and return the total 